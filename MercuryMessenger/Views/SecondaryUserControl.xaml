<UserControl
    x:Class="MercuryMessenger.Views.SecondaryUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="{DynamicResource SecondaryUserControl.Background}"
    BorderBrush="{DynamicResource SecondaryUserControl.BorderBrush}"
    BorderThickness="2"
    mc:Ignorable="d">
    <Grid>
        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="15"
            Foreground="{DynamicResource SecondaryUserControl.Foreground}"
            Text="{Binding Message}" />
        <Button
            Height="40"
            Margin="5"
            VerticalAlignment="Bottom"
            Command="{Binding RegisterButtonClickedCommand}"
            Content="{Binding ButtonContent}" />
    </Grid>
</UserControl>
